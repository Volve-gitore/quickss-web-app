export type IState = {
    clientId: string,
    groupId: string,
    type: string,
    categoryId: string,
    name: string,
    currency: string,
    price: number,
    flag: number,
    description: string,
    images: any,
  }